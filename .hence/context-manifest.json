{
  "task_id": "theme-toml-parsing",
  "token_budget": 8000,
  "tokens_used": 624,
  "elements_selected": [
    {
      "source": "plan-meta/description",
      "content": "Add theme manifest parsing. Create a new module src/theme.rs (or src/web/theme.rs) with:\n\n```rust\n#[derive(Debug, Clone, Serialize, Deserialize)]\npub struct ThemeManifest {\n    pub theme: ThemeInfo,\n}\n\n#[derive(Debug, Clone, Serialize, Deserialize)]\npub struct ThemeInfo {\n    pub name: String,\n    pub version: String,\n    #[serde(default)]\n    pub description: Option<String>,\n    #[serde(default)]\n    pub author: Option<String>,\n    #[serde(default)]\n    pub license: Option<String>,\n    #[serde(default)]\n    pub homepage: Option<String>,\n    #[serde(default)]\n    pub min_zetl_version: Option<String>,\n    #[serde(default)]\n    pub templates: Option<ThemeTemplates>,\n}\n\n#[derive(Debug, Clone, Serialize, Deserialize)]\npub struct ThemeTemplates {\n    #[serde(default)]\n    pub overrides: Vec<String>,\n}\n```\n\nAdd `toml` crate to Cargo.toml (use latest stable, likely 0.8+).\n\nPublic API:\n  - parse_theme_manifest(content: &str) -> Result<ThemeManifest>\n    Parses TOML content. Validates name matches ^[a-z0-9][a-z0-9_-]*$ and version is valid SemVer (use the `semver` crate or a simple regex\n  - load_theme_manifest(theme_dir: &Path) -> Option<ThemeManifest>\n    Reads theme.toml from the given directory. Returns None if file doesn't exist. Returns error context if file exists but is malformed.\n  - load_bundled_manifest(theme_name: &str) -> Option<ThemeManifest>\n    Reads theme.toml from the embedded theme directory.\n  - validate_theme_name(name: &str) -> Result<()>\n    Validates the name pattern. Returns descriptive error on failure.\n\nAlso parse .zetl-source.toml (provenance) with a separate struct — needed for theme list in the next task:\n\n```rust\n#[derive(Debug, Clone, Serialize, Deserialize)]\npub struct ThemeSource {\n    pub source: ThemeSourceInfo,\n}\n\n#[derive(Debug, Clone, Serialize, Deserialize)]\npub struct ThemeSourceInfo {\n    pub url: String,\n    #[serde(default)]\n    pub ref_name: Option<String>,\n    pub commit: String,\n    #[serde(default)]\n    pub path: Option<String>,\n    pub installed_at: String,\n    pub zetl_version: String,\n}\n```\n\nREQ-014-001, REQ-014-002, CON-014-001.",
      "token_cost": 527,
      "priority": "PlanMeta"
    },
    {
      "source": "memory/episodic/task-extract-default-theme-changes",
      "content": "Modified 5 files (+998, -0): themes/default/base.html, themes/default/folder.html, themes/default/index.html, themes/default/page.html, themes/default/theme.toml",
      "token_cost": 40,
      "priority": "AgentMemory"
    },
    {
      "source": "memory/episodic/task-compile-time-embedding-changes",
      "content": "Modified 8 files (+86, -1003): .hence/context-manifest.json, Cargo.lock, Cargo.toml, src/web/engine.rs, src/web/templates/base.html and 3 more",
      "token_cost": 35,
      "priority": "AgentMemory"
    },
    {
      "source": "memory/episodic/task-three-tier-resolution-changes",
      "content": "Modified 3 files (+57, -21): .hence/context-manifest.json, src/main.rs, src/web/engine.rs",
      "token_cost": 22,
      "priority": "AgentMemory"
    }
  ],
  "elements_excluded": [],
  "constructed_at": "2026-03-02T06:21:13Z"
}