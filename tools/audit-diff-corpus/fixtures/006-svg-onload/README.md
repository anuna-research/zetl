# 006-svg-onload

**Scenario:** inline `<svg onload=...>` — a favourite because it's
short, parses as HTML, and historically bypassed under-built
sanitisers.

**Expected:** `sanitiser-stripped`.

**Source:** OWASP XSS cheatsheet (SVG vector).
