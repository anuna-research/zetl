# 001-unseen-domain

**Scenario:** PR introduces a link to a domain the vault has never
referenced before.

**Expected:** `unseen-domain` fires on the new link; the baseline
domain does not.

**Source:** hand-authored.
