# 004-data-uri-img

**Scenario:** `data:` URI inside a markdown image — the classic
SVG-onload exfil vector.

**Expected:** `dangerous-scheme`.

**Source:** OWASP XSS cheatsheet (data-URI SVG variant).
