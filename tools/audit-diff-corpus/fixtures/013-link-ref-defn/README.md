# 013-link-ref-defn

**Scenario:** reference-style link whose *definition* (far from its
use site in prose) points at a previously-unseen host. Gate must
resolve references, not only inline links.

**Expected:** `unseen-domain`.

**Source:** CommonMark spec §link reference definitions.
