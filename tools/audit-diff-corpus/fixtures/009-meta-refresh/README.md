# 009-meta-refresh

**Scenario:** `<meta http-equiv=refresh>` redirecting to an unseen
external host. Redirects straight to the attacker's site on load.

**Expected:** `sanitiser-stripped`.

**Source:** OWASP XSS cheatsheet (meta-refresh variant).
