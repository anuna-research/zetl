{
  "name": "@zetl/cap-shim",
  "version": "0.4.0",
  "description": "zetl capability-mode browser shim (SPEC-034 CON-3408). Private — bundled into /assets/shim.js at build time.",
  "private": true,
  "license": "AGPL-3.0-or-later",
  "type": "module",
  "main": "./index.ts",
  "scripts": {
    "build": "node build.mjs",
    "typecheck": "tsc --noEmit",
    "test": "node --experimental-transform-types --no-warnings --test 'test/*.test.ts'"
  },
  "dependencies": {
    "@noble/ed25519": "^2.1.0",
    "@noble/hashes": "^1.6.1",
    "age-encryption": "^0.2.1"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "esbuild": "^0.24.0",
    "happy-dom": "^15.11.7",
    "typescript": "^5.7.0"
  },
  "engines": {
    "node": ">=18"
  }
}
