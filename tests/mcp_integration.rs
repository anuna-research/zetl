//! Integration tests for MCP server (SPEC-021).
//! Requires: cargo test --features mcp

#[test]
fn mcp_feature_compiles() {
    // Smoke test — if this compiles, the feature gate works
    assert!(true);
}
