# mdbook-katex canary

Inline math: the Pythagorean identity $a^2 + b^2 = c^2$ sits beside
prose and must not break paragraph flow.

Display math on its own line:

$$ e^{i\pi} + 1 = 0 $$

Paragraph after the display block.
